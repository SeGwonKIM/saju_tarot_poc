# PoC PRD — 사주 상담 접수·리포트 초안 자동화

> **Main Quest 2** 제출용 기술 명세 · 작성 2026-09-22
> 문제 정의는 `문제정의서.md`, 상용 구상은 `세권 — … 제품기획서 (전문가 감수본).docx`

---

## 0. 문서 구조와 과제 요건 대응

이 프로젝트의 문서는 셋으로 나뉩니다. **성격이 달라서 한 문서에 담을 수 없습니다.**

| 문서 | 무엇 | 기간 |
|---|---|---|
| **제품기획서 (.docx)** | 상용 서비스 전체 구상 — 사주·타로·궁합·오늘운세·택일 | 8주 MVP 이후 |
| **문제정의서.md** | 과제 제출물 ① — 도메인·문제·가설·사용자·성공 기준 | — |
| **PRD_PoC.md** (이 문서) | **PoC 구현 명세** | **2주** |

### 과제 요건 대응표

| 과제가 요구하는 것 | 위치 |
|---|---|
| 문제 정의서 | `문제정의서.md` |
| AI 모델 선정 근거 | **2장** |
| PoC 구현 (입력 → AI 처리 → 결과 출력) | **3·4장** |
| 개선 효과 검증 (기존 방식 비교, 실패 사례) | **5장** |
| 실행 환경 | **6장** |
| 한계·다음 스텝 | **8장** |
| 제출물 4종 | **7장 체크리스트** |

⚠️ **이 단계는 PoC 입니다.** 회원가입·결제·배포·화면은 만들지 않습니다.

---

## 1. 한 줄 요약

```
상담 녹취(음성) → 사주 리포트 초안(문서) 까지 명령 한 줄로
```

---

## 2. AI 모델 선정과 근거

### 2.1 후보 비교

| 모델·기법 | 이 도메인에 쓸 자리 | 적합도 | 판정 |
|---|---|---|---|
| **STT (Whisper)** | 상담 녹취 → 텍스트 | **높음** — 받아 적기가 실제 병목 | **채택** |
| **LLM** | 항목 추출 + 리포트 초안 서술 | **높음** — 자유 발화 구조화, 문장 생성 | **채택** |
| OCR / VLM | 종이 역학 자료 디지털화 | 중간 — 유용하나 이번 병목은 아님 | 보류 |
| Diffusion | 리포트 표지 이미지 | 낮음 — 문제 해결과 무관 | 제외 |
| YOLO / SAM | 없음 | 낮음 — 이 도메인에 객체·영역이 없음 | 제외 |

### 2.2 STT 를 고른 이유

| 이유 | 설명 |
|---|---|
| **병목의 맨 앞** | 잘못 받아 적으면 뒤의 모든 계산이 틀어짐. 가장 비싼 실수 자리 |
| **도구가 이미 있음** | `whisper_mcp/transcribe.py` — 명령줄·MCP 양쪽으로 쓰게 만들어 둔 것 |
| **데이터를 만들 수 있음** | 상담 상황을 직접 녹음해 쓸 수 있음 |
| **GPU 없이도 됨** | 작은 모델은 CPU, 큰 모델이 필요하면 Colab |

### 2.3 안 고른 것의 이유

**Diffusion 을 넣지 않았습니다.** 넣으면 결과물은 화려해지지만 **문제정의서 2장의 문제를 하나도 풀지 못합니다.** 과제가 요구하는 것은 모델을 많이 쓰는 것이 아니라 **문제에 맞는 것을 고르는 것**입니다.

---

## 3. PoC 범위

### 3.1 하는 것과 하지 않는 것

| 하는 것 | 하지 않는 것 |
|---|---|
| 상담 녹취 1건을 받아 텍스트화 | 회원가입·로그인 |
| 텍스트에서 이름·성별·생년월일시·고민 추출 | 결제·PG 연동 |
| 음양력 변환·진태양시·서머타임 보정 | 웹 배포·도메인 |
| 원국 8글자와 세운·월운 산출 | 화면 디자인 |
| ~~대운 산출~~ — ⚠ **엔진 미구현 (9장 P-6)** | |
| 리포트 초안 생성 | 타로·궁합·택일 |
| 결과를 문서 한 장으로 출력 | 관리자 기능 |
| 명령줄에서 한 번에 실행 | 작명·개명 (범위에서 제외됨) |

### 3.2 파이프라인

```
① 상담 녹취 (.m4a / .wav)
        │  [AI · Whisper STT]          whisper_mcp/transcribe.py
        ▼
② 상담 텍스트
        │  [AI · LLM 항목 추출]         새로 짬
        ▼
③ 구조화 입력  이름·성별·생년월일·시간·음양력·윤달·출생지·고민
        │  [코드]                       calendar_service · korea_time
        ▼
④ 보정된 출생 시각
        │  [코드]                       saju_service.build_chart / current_period
        ▼
⑤ 원국 8글자 · 오행 분포 · 세운/월운   (대운은 미구현)
        │  [코드]                       report_service.build_facts
        ▼
⑥ 해석용 사실 묶음 (JSON)
        │  [AI · LLM]                   report_service.OpenAIReportGenerator
        ▼
⑦ 리포트 초안 + 금칙어 검사             report_service.has_banned_word
```

**AI 가 계산하는 구간이 없습니다.** ③~⑥은 전부 결정론적 코드입니다.

### 3.3 기존 코드 재사용 — 새로 짤 게 적습니다

| 있는 것 | 위치 | 하는 일 |
|---|---|---|
| `lunar_to_solar` · `solar_to_lunar` · `has_leap_month` | `backend/app/calendar_service.py` | 음양력·윤달 |
| `correct` · `is_dst` · `standard_meridian` | `backend/app/korea_time.py` | 진태양시·서머타임 |
| `build_chart` · `current_period` | `backend/app/saju_service.py` | 원국·오행·**세운/월운** ⚠ 대운은 미구현 |
| `build_facts` · `OpenAIReportGenerator` · `has_banned_word` | `backend/app/report_service.py` | 해석 사실·LLM 리포트·금칙어 |
| `run()` + CLI | `whisper_mcp/transcribe.py` | 받아쓰기 |

> **새로 짜는 것은 ② 항목 추출과 전체를 잇는 진입점, 둘뿐입니다.**
> 리포트 생성기와 금칙어 검사까지 이미 있습니다.

---

## 4. 구현 명세

### 4.1 실행 형태

```bash
python poc/intake.py --audio samples/상담_001.m4a --out out/
```

음성이 없을 때를 위한 텍스트 경로도 둡니다.

```bash
python poc/intake.py --text samples/상담_001.txt --out out/
```

### 4.2 입력과 출력

| | 내용 |
|---|---|
| **입력** | 상담 녹취 1건 (또는 같은 내용의 텍스트) |
| **중간 산출** | `transcript.txt` · `input.json` · `chart.json` · `facts.json` |
| **최종 출력** | `리포트_초안.md` + `run.json` (단계별 소요 시간·모델·버전) |

중간 산출을 전부 파일로 남기는 이유는 **어느 단계에서 틀렸는지 알기 위해서**입니다.

### 4.3 ② 항목 추출 — 뽑을 것

| 필드 | 필수 | 못 뽑았을 때 |
|---|---|---|
| `name` | O | 물어보도록 표시 |
| `gender` | O | 대운 순행·역행에 필요 — ⚠ 대운이 아직 없어 **현재는 쓰이지 않음** |
| `birth_date` | O | 없으면 진행 불가 |
| `calendar_type` · `is_leap` | O | 기본 양력. 음력이면 윤달 여부 확인 |
| `birth_time` | △ | 없으면 3주 6글자 모드 |
| `birth_place` | △ | 없으면 서울 기준 경도로 보정 |
| `question` | △ | 없어도 진행 |

**LLM 에는 "모르면 null 로 두라"** 고 지시합니다. 지어내면 뒤가 전부 틀어집니다.

### 4.4 폴더 구조

```
saju_tarot/
├─ poc/
│  ├─ intake.py          실행 진입점
│  ├─ stt.py             ① whisper_mcp 호출
│  ├─ extract.py         ② LLM 항목 추출        ← 새로 짬
│  └─ pipeline.py        ③~⑦ 기존 서비스 연결   ← 새로 짬
├─ samples/              상담 녹취 · 기준선 기록
└─ out/                  결과물
```

---

## 5. 개선 효과 검증 계획

### 5.1 정량 — 기존 방식과 나란히

**같은 1건을 두 방식으로 처리하고 단계별 시간을 잽니다.**

| 단계 | 수작업 | PoC |
|---|---|---|
| 상담 받아 적기 | 측정 | 측정 |
| 음양력 변환 | 측정 | 측정 |
| 진태양시·서머타임 보정 | 측정 | 측정 |
| 원국·세운 산출 | 측정 | 측정 |
| 해석 초안 작성 | 측정 | 측정 |
| **합계** | **기준선** | **S3 판정** |

정확도는 표본으로 잽니다.

| 항목 | 표본 | 통과 기준 |
|---|---|---|
| STT 항목 추출 | 녹취 5건 × 5항목 = 25개 | 20개 이상 |
| 만세력 원국 | 20건 | 8글자 전부 일치 |
| 재현성 | 같은 녹취 3회 | 원국·구조 동일 |

### 5.2 정성 — 초안과 최종본 대조

리포트 초안을 사람이 다듬어 최종본을 만들고, **얼마나 손댔는지** 봅니다.

| 볼 것 | 어떻게 |
|---|---|
| 그대로 쓴 문단 / 고친 문단 / 버린 문단 | 개수로 셈 |
| 사실과 어긋난 서술이 있었나 | 있으면 전부 기록 |
| 톤이 맞았나 | 단정적 표현이 나왔는지 (`has_banned_word` 로 1차 검사) |

### 5.3 실패 사례 수집 — 필수

과제가 명시적으로 요구합니다. 처음부터 모을 자리를 만듭니다.

| 예상되는 실패 | 왜 생기나 |
|---|---|
| "아침 여덟시쯤" 같은 모호한 시각 | 자유 발화의 모호함 |
| "음력 칠월 스무날" 에서 윤달 여부가 빠짐 | 물어보지 않으면 알 수 없음 |
| 사투리·잡음이 섞인 녹취 | STT 한계 |
| 출생지를 안 말해 보정이 서울 기준이 됨 | 기본값의 한계 |
| LLM 이 없는 값을 지어냄 | 추출 프롬프트 실패 |
| 리포트가 원국과 어긋난 서술을 함 | 근거 주입 실패 |

**실패를 감추지 않고 목록으로 제출합니다.** 한계를 아는 것도 성과라고 과제에 적혀 있습니다.

---

## 6. 실행 환경

| | |
|---|---|
| 기본 | 로컬 (Windows 11, Python 3.13) |
| GPU 필요 시 | **Colab 무료 T4** — Whisper large 모델을 쓸 경우 |
| LLM | API 호출. 키는 `.env` 로 분리하고 저장소에 올리지 않음 |
| 배포 | **하지 않음** (과제에서 필수 아님) |

⚠️ **개인정보** — 상담 녹취에는 실명·생년월일시가 들어갑니다.
저장소에 올리는 샘플은 **가명·가상 생년월일로 바꾼 것**만 씁니다. 실제 의뢰인 녹취는 올리지 않습니다.

---

## 7. 2주 일정과 제출물

### 일정

| 일차 | 할 일 | 끝났는지 아는 법 |
|---|---|---|
| 1일 | **기준선 측정** — 수작업으로 1건을 처리하며 단계별 시간 기록 | 5.1 왼쪽 칸이 채워짐 |
| 2일 | **S2 먼저** — 기존 만세력 엔진을 표본 20건으로 검증 | 8글자가 전부 일치 |
| 3~4일 | 상담 녹취 샘플 5건 만들기 (가상 의뢰인) | 음성 파일 5개 |
| 5일 | STT 연결 | 녹취에서 텍스트가 나옴 |
| 6~7일 | ② 항목 추출 구현 | 5항목이 JSON 으로 나옴 (S1) — ✅ **2일차에 앞당겨 완료, 숨김 14/15** |
| 8~9일 | ③~⑥ 기존 서비스 연결 | 원국·세운·facts 가 나옴 (✅ 2일차 완료) |
| 10~11일 | ⑦ 리포트 초안 + 금칙어 검사 | 문서 한 장이 나옴 |
| 12일 | 재현성·시간 측정 | S3·S4 판정 |
| 13일 | 정성 검증과 실패 사례 정리 | 5.2·5.3 표가 채워짐 |
| 14일 | README·시연 자료 정리 | 아래 체크리스트 통과 |

### 제출물 체크리스트

| | 제출물 | 위치 | 상태 |
|---|---|---|---|
| ☐ | **문제 정의서** | `docs/문제정의서.md` | **작성 완료** |
| ☐ | **PoC 코드** (모델 선정 근거 포함) | `poc/` + 이 문서 2장 | |
| ☐ | **개선 효과 검증 결과** | `개선효과검증결과.md` — 5장 표를 채운 것 | |
| ☐ | **README** (실행 방법 + 시연 자료) | `README.md` | |
| ☐ | GitHub 저장소 URL | | |

---

## 8. 한계와 다음 스텝

### 8.1 이 PoC 가 답하지 못하는 것

| 답하지 못하는 것 | 왜 |
|---|---|
| 고객이 돈을 낼 것인가 | 결제를 만들지 않았으므로 |
| 여러 건을 동시에 처리할 때 견디는가 | 1건 기준으로만 만들었으므로 |
| 해석의 품질이 전문가 수준인가 | 초안이 목표이지 최종본이 아니므로 |
| 타 사이트와 만세력이 정말 다 같은가 | 표본 20건으로만 봤으므로 |

### 8.2 통과·중단 판정

시작 전에 적어 둡니다.

| 판정 | 조건 | 다음 행동 |
|---|---|---|
| **통과** | S1~S5 가 모두 목표를 넘음 | 제품기획서 5장 8주 MVP 로 진입 |
| **조건부** | S5(초안 품질)만 부족 | 접수·산출 자동화만 쓰고 해석은 사람이 |
| **보류** | S2(만세력)가 막힘 | 엔진부터 고치고 재판정 |
| **중단** | S1(항목 추출)이 절반도 안 됨 | 음성 입력을 접고 폼 입력으로 전환 |

**중단도 결과입니다.** 두 주 만에 알아내는 것이 상용 개발 뒤에 알아내는 것보다 훨씬 쌉니다.

### 8.3 상용으로 가는 길

```
PoC (2주)  →  과제 제출  →  8주 MVP (기획서 5장)  →  상용 v1.x
                                                      └ 택일 (기획서 7장)
                                                      └ 벤치마크 보강 (기획서 8장)
```

---

## 9. 결정이 필요한 항목

| 번호 | 결정할 것 | 기본값 |
|---|---|---|
| P-1 | AI 모델 | **STT(Whisper) + LLM** |
| P-2 | 상담 녹취를 어떻게 만들지 | 가상 의뢰인 역할극을 직접 녹음 |
| P-3 | Whisper 모델 크기 | 먼저 작은 것으로. 정확도가 모자라면 Colab 에서 large |
| P-4 | 리포트 주제 범위 | ~~기존 `report_service` 의 topics 를 그대로~~ → **불가**. topics 5개가 샘플 고민과 하나도 안 맞아 `poc/report.py` 에 9개로 새로 둠 |
| P-5 | 원본 기획서 1~2장의 작명 문구 | **완료 (2026-09-22)** — 3곳 모두 제거. 기획서에 작명·개명 언급 0곳 |
| **P-6** | **대운을 PoC 범위에 넣을 것인가** (→ 10장 D-5) | 엔진에 없음. 넣으려면 새로 구현해야 함. 빼면 `gender` 도 당장은 쓸 데가 없음 |
| **P-7** | **S4 보강을 어디까지** | 숫자 대조·근거 인용·규칙 교차·되읽기 확인 중 어디까지 (검증결과.md S4 참조) |

---

## 10. 기술 부채 상환 계획

> PoC 는 빨리 답을 얻으려고 일부러 진 빚이 있습니다. **무엇을 빌렸고 언제 갚을지**를
> 적어 두지 않으면 상용 전환 때 그대로 사고가 됩니다.

| # | 빌린 것 | 왜 빌렸나 | 갚는 방법 | 언제 |
|---|---|---|---|---|
| **D-1** | `poc/report.py` 가 상용 `report_service.py` 와 **갈라짐** | 상용 쪽은 타로 3장이 필수이고 주제 enum 이 안 맞아 그대로 못 씀 (F-9) | **PoC 파일을 버리고** `report_service` 에 **사주 전용 분기**를 넣어 통합. 공통 로직(Report·금칙어·스키마 강제)은 공유 모듈로 추출 | 상용 착수 즉시 |
| **D-2** | `engine/` 이 상용 저장소의 **복사본** | 이 저장소만 클론해도 돌아가게 하려고 | **패키지로 분리**하거나 서브모듈로 바꿔 한쪽만 고쳐도 양쪽에 반영되게. 지금은 F-8 을 PoC 에서 고쳐도 **상용에 안 반영됨** | 상용 착수 즉시 |
| **D-3** | 금칙어를 `poc/safety.py` 에 **따로 구현** | 상용 `BANNED` 11개가 과차단·미차단을 동시에 냄 (F-8·F-19) | `safety.py` 를 상용으로 **올려보내고** 상용의 낱말 목록을 폐기. 규칙별 차단/통과 쌍 테스트도 함께 이관 | D-1 과 동시 |
| **D-4** | `transcript.txt` 에 **원문을 그대로 저장** | 어느 단계에서 틀렸는지 보려고 | **보관 기간·파기 규칙**을 정하고 자동 파기. 음성 원본은 STT 완료 후 즉시 파기 | 실제 녹취를 쓰기 **전에** |
| **D-5** | 대운 **미구현**인데 `gender` 는 받고 있음 | 엔진에 없음 (F-12·F-13) | 대운을 구현하거나(P-6), 안 할 거면 `gender` 를 슬롯에서 빼고 S1 채점 항목도 줄임 | P-6 결정 후 |
| **D-6** | 받아쓰기를 **CPU** 로 돌림 | GPU 환경이 없었음 | GPU 또는 Colab. 지금은 실시간의 1.1~1.6배라 **10분 상담에 11~16분** — 상용에서는 못 씁니다 | 상용 착수 즉시 |
| **D-7** | 개인정보 **마스킹만** 있고 흐름도·동의·파기 문서가 없음 | PoC 는 가상 표본이라 실제 PII 가 없었음 | 데이터 흐름도 · 목적별 동의 분리 · 국외 이전 고지 · 파기 규칙. **법률 검토 필요** | 실제 녹취를 쓰기 **전에** |
| **D-8** | 표본이 전부 **작성자가 만든 가상 대화** | 실제 녹취를 구할 수 없었음 | 제3자가 만든 **잠금 시험셋** 확보 (F-23) | 제출 전 가능하면, 늦어도 상용 전 |

### 갚는 순서

```
실제 녹취를 쓰기 전에 반드시 :  D-4  D-7          (개인정보 — 어기면 사고)
상용 착수 즉시               :  D-1  D-2  D-3  D-6  (안 하면 검증이 무의미해짐)
결정에 달린 것               :  D-5  D-8
```

**D-2 가 가장 조용하고 위험합니다.** PoC 에서 고친 F-8(금칙어 오작동)이 상용 코드에는
**반영돼 있지 않습니다.** 복사본을 고쳤을 뿐이기 때문입니다. 이 상태로 상용에 가면
"PoC 에서 검증했다"는 말이 성립하지 않습니다.
